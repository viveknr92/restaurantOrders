version: '2.0'

services:
  angular:
    build: client
    container_name: frontend
    ports:
      - "4200:4200"
    network_mode: "host"
    depends_on:
      - express

  express:
    build: backend
    container_name: backend
    ports:
      - "3000:3000"
    network_mode: "host"
    depends_on:
      - database

  
  database:
    image: mongo
    container_name: db
    ports:
      - "27017:27017"
    network_mode: "host"
    volumes: 
      - ./mongo-volume:/data/db